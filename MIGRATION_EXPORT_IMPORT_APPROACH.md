# Подход к миграции через Export/Import PRS файлы

## Дата: 2025-01-27

## ✅ Обнаружено

### 1. Действия Export/Import доступны
- **Действия**: `A_EXPORT = "export"` и `A_IMPORT = "import"` в `EditableChildrenContextConstants`
- **Расширение**: `.prs` (Properties File)
- **Доступность**: В контекстах типа `EditableChildrenContext` (models, devices, widgets, dashboards)
- **Тест**: Действие `export` успешно выполнилось

### 2. Проблема с интерактивностью
- Действия требуют интерактивного взаимодействия (диалог выбора файла)
- Нужно найти способ автоматизации

## 💡 Решения

### Вариант 1: Модификация ExecuteActionTool для обработки SelectFile
Действия export/import, вероятно, используют команду `SelectFile` для выбора файла. Можно:
1. Расширить `ExecuteActionTool` для обработки `SelectFile`
2. Автоматически возвращать путь к файлу из параметров
3. Обрабатывать команды `SelectFile` в `processCommand`

### Вариант 2: Использование функций напрямую (если доступны)
Найти функции, которые работают напрямую с файлами без диалогов.

### Вариант 3: Гибридный подход
- Использовать export/import для простых контекстов (без сложной структуры)
- Использовать прямой подход для моделей (с типом, компонентами, группами)

## 📋 План реализации

### Шаг 1: Расширение ExecuteActionTool
1. Добавить обработку команды `SelectFile` в `processCommand`
2. Автоматически возвращать путь к файлу из параметров действия
3. Протестировать на одном контексте

### Шаг 2: Тестирование Export/Import
1. Экспортировать контекст с исходного сервера в `.prs` файл
2. Импортировать контекст на целевой сервер из `.prs` файла
3. Проверить, что все данные перенесены правильно (тип модели, компоненты, группа)

### Шаг 3: Автоматизация
1. Создать скрипт для экспорта всех контекстов
2. Создать скрипт для импорта всех контекстов
3. Применить ко всем контекстам

## ⚠️ Важные замечания

1. **Формат PRS файла**: Нужно убедиться, что PRS файл содержит все необходимые данные:
   - Тип модели (абсолютная, относительная, экземплярная)
   - Переменные модели
   - Функции модели
   - События модели
   - Привязки
   - Наборы правил
   - Группа контекста

2. **Группы**: Нужно убедиться, что группы сохраняются и восстанавливаются правильно

3. **Кодировка**: Убедиться, что UTF-8 кодировка сохраняется в PRS файлах

## 🔄 Следующие шаги

1. Расширить `ExecuteActionTool` для обработки `SelectFile`
2. Протестировать export/import одного контекста
3. Проверить содержимое PRS файла
4. Применить ко всем контекстам

