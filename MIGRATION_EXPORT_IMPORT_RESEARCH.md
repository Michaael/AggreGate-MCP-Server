# Исследование Export/Import для миграции

## Дата: 2025-01-27

## ✅ Найденная информация

### 1. Действия Export/Import
- **Действия**: `A_EXPORT = "export"` и `A_IMPORT = "import"` в `EditableChildrenContextConstants`
- **Расширение файла**: `.prs` (Properties File Extension)
- **Доступность**: В контекстах типа `EditableChildrenContext` (models, devices, widgets, dashboards)
- **Тест**: Действие `export` успешно выполнилось на `users.admin.models`

### 2. Функция exportContext
- **Функция**: `F_EXPORT_CONTEXT = "exportContext"` в `ApplicationContextConstants`
- **Назначение**: Для Application контекстов (не для обычных моделей)

### 3. Проблема
- Действия требуют интерактивного взаимодействия (диалог выбора файла)
- Нужно найти способ автоматизации

## 🔍 Что нужно найти

1. **Реализация действий в клиенте**:
   - Как клиент обрабатывает `export`/`import`
   - Какие команды отправляются на сервер
   - Можно ли передать путь к файлу напрямую

2. **Функции для прямого экспорта/импорта**:
   - Функции, которые работают без диалогов
   - Параметры (путь к файлу, формат)

3. **Формат PRS файла**:
   - Структура файла
   - Как хранятся данные контекста

## 💡 Возможные решения

### Вариант 1: Модификация ExecuteActionTool
- Расширить `ExecuteActionTool` для обработки команд выбора файла
- Автоматически выбирать файл из параметров

### Вариант 2: Прямые функции
- Найти функции, которые работают напрямую с файлами
- Использовать их вместо действий

### Вариант 3: Гибридный подход
- Использовать export/import для простых контекстов
- Использовать прямой подход для сложных (модели)

## 📋 План действий

1. ✅ Найти действия export/import
2. ⏳ Найти реализацию в клиенте
3. ⏳ Найти функции для прямого экспорта/импорта
4. ⏳ Протестировать экспорт/импорт одного контекста
5. ⏳ Применить ко всем контекстам

