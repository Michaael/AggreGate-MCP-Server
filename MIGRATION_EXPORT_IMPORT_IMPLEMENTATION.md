# Реализация Export/Import для миграции

## Дата: 2025-01-27

## ✅ Выполнено

### 1. Расширение ExecuteActionTool
- ✅ Добавлен параметр `filePath` в схему входных данных
- ✅ Добавлена обработка `filePath` в методе `execute()`
- ✅ Расширен `processCommand()` для обработки пути к файлу в export/import действиях
- ✅ Автоматическое определение поля для пути к файлу (ищет поля с "file", "path", "name")
- ✅ Создание новой таблицы данных, если она отсутствует

### 2. Логика обработки файлов
- При получении `EditData` команды для export/import действий:
  1. Проверяется наличие `filePath` параметра
  2. Ищется поле в таблице данных, которое может содержать путь к файлу
  3. Устанавливается путь к файлу в найденное поле
  4. Если таблица данных отсутствует, создается новая с полем "file"

## 📋 Использование

### Экспорт контекста в файл

```json
{
  "path": "users.admin.models.objects",
  "actionName": "export",
  "filePath": "C:/export/objects.prs",
  "connectionKey": "source"
}
```

### Импорт контекста из файла

```json
{
  "path": "users.admin.models",
  "actionName": "import",
  "filePath": "C:/export/objects.prs",
  "connectionKey": "target"
}
```

## 🔍 Детали реализации

### Обработка EditData команды

1. **Получение данных**: Извлекается таблица данных из `EditData.CF_DATA`
2. **Поиск поля**: Ищется поле, которое может содержать путь к файлу:
   - Поля с именами, содержащими "file", "path", или равные "name"
   - Тип поля должен быть STRING
3. **Установка пути**: Устанавливается значение `filePath` в найденное поле
4. **Создание данных**: Если таблица данных отсутствует, создается новая с полем "file"

### Логирование

- Логируется тип команды и параметры
- Логируется установка пути к файлу
- Логируются ошибки при установке пути

## ⚠️ Ограничения

1. **Структура данных**: Автоматическое определение поля для пути к файлу может не работать, если структура данных отличается от ожидаемой
2. **Формат файла**: Файл должен быть в формате `.prs` (Properties File)
3. **Права доступа**: Необходимы права на запись в указанный путь

## 🎯 Следующие шаги

1. ⏳ Пересобрать MCP сервер
2. ⏳ Протестировать export на одном контексте
3. ⏳ Протестировать import на целевом сервере
4. ⏳ Проверить корректность переноса данных
5. ⏳ Применить к миграции моделей

## 📝 Примеры использования

### Экспорт модели objects

```python
# Экспорт
result = mcp_aggregate_aggregate_execute_action(
    path="users.admin.models",
    actionName="export",
    filePath="C:/migration/export/objects.prs",
    connectionKey="source"
)

# Импорт
result = mcp_aggregate_aggregate_execute_action(
    path="users.admin.models",
    actionName="import",
    filePath="C:/migration/export/objects.prs",
    connectionKey="target"
)
```

### Массовый экспорт моделей

1. Получить список всех моделей
2. Для каждой модели:
   - Экспортировать в файл `{modelName}.prs`
   - Сохранить в директорию `C:/migration/export/models/`
3. Импортировать все файлы на целевом сервере

