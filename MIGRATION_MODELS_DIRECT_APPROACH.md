# Прямой подход для миграции моделей

## Дата: 2025-01-27

## ✅ Полученные данные модели objects

### childInfo (тип модели):
- **name**: objects
- **description**: объекты
- **type**: 1 (TYPE_ABSOLUTE - абсолютная модель)
- **enabled**: true
- **protected**: false
- **containerType**: objects
- **containerName**: objects
- **objectType**: object
- И другие параметры...

### info (группа):
- **group**: default
- **type**: model
- **description**: объекты

### modelVariables:
- 4 переменные:
  - abonent (абонент)
  - flat (адрес квартиры)
  - guideAbonent (справочник абонентов)
  - guideFlat (справочник квартир)

### modelFunctions:
- 1 функция:
  - setAbonent (добавить случайного абонента)

## 📋 План переноса модели

### Шаг 1: Получение данных с исходного сервера
1. Получить `childInfo` (тип модели, параметры)
2. Получить `info` (группа)
3. Получить `modelVariables` (переменные модели)
4. Получить `modelFunctions` (функции модели)
5. Получить `modelEvents` (события модели)
6. Получить `bindings` (привязки)
7. Получить `ruleSets` (наборы правил)

### Шаг 2: Создание модели на целевом сервере
1. Создать контекст модели через `create_context`
2. Установить `childInfo` с правильным типом
3. Установить `info` с правильной группой

### Шаг 3: Установка компонентов
1. Установить `modelVariables`
2. Установить `modelFunctions`
3. Установить `modelEvents`
4. Установить `bindings`
5. Установить `ruleSets`

## 🎯 Типы моделей для тестирования

1. **Абсолютная (TYPE_ABSOLUTE = 1)**: objects ✅
2. **Относительная (TYPE_RELATIVE = 0)**: нужно найти
3. **Экземплярная (TYPE_INSTANTIABLE = 2)**: нужно найти

