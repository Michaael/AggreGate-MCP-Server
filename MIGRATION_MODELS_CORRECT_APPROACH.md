# Правильный подход к миграции моделей

## Дата: 2025-01-27

## ✅ Успешный тест с моделью `objects`

Модель `objects` успешно пересоздана с правильной структурой:

1. ✅ **Тип модели**: `type = 1` (TYPE_ABSOLUTE) - установлен через `childInfo`
2. ✅ **Переменные модели**: 4 переменные установлены через `modelVariables`
3. ✅ **Функции модели**: 1 функция установлена через `modelFunctions`
4. ✅ **Группы**: Все 30 групп моделей созданы

## 📋 Правильный процесс миграции модели

Для каждой модели нужно выполнить:

### Шаг 1: Получение информации
1. Получить `childInfo` - для определения типа модели
2. Получить `info` - для определения группы
3. Получить `modelVariables` - переменные модели
4. Получить `modelFunctions` - функции модели
5. Получить `modelEvents` - события модели
6. Получить `bindings` - привязки
7. Получить `ruleSets` - наборы правил

### Шаг 2: Создание модели
1. Удалить старую модель (если существует)
2. Создать модель через `create_context`
3. Установить `childInfo` с правильным типом через `set_variable`
4. Установить `modelVariables` через `set_variable`
5. Установить `modelFunctions` через `set_variable`
6. Установить `modelEvents` через `set_variable` (если есть)
7. Установить `bindings` через `set_variable` (если есть)
8. Установить `ruleSets` через `set_variable` (если есть)
9. Установить группу через `set_variable_field` для `info.group` (если не "default")

## ⚠️ Важные замечания

1. **Типы моделей**:
   - `0` = TYPE_RELATIVE (относительная)
   - `1` = TYPE_ABSOLUTE (абсолютная)
   - `2` = TYPE_INSTANTIABLE (экземплярная)

2. **Группы**: Модели могут находиться в группах (не только "default"). Нужно получить группу из `info.group` и установить её после создания модели.

3. **Формат данных**: Компоненты моделей имеют сложную вложенную структуру DataTable, но их можно переносить напрямую через `set_variable`.

## 📊 Статус

- ✅ Подход протестирован на модели `objects`
- ⏳ Применение ко всем 102 моделям - в процессе

