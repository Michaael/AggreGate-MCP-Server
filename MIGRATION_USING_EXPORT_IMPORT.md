# Миграция с использованием Export/Import в PRS файлы

## Дата: 2025-01-27

## ✅ Обнаружено

### 1. Действия Export/Import
- ✅ В `EditableChildrenContextConstants` есть действия:
  - `A_EXPORT = "export"` - экспорт контекста в файл
  - `A_IMPORT = "import"` - импорт контекста из файла
- ✅ Расширение файла: `.prs` (Properties File)
- ✅ Действие `export` успешно выполнилось на контексте `users.admin.models`

### 2. Структура
- Действия доступны в контекстах типа `EditableChildrenContext` (например, `models`, `devices`, `widgets`, `dashboards`)
- Файлы имеют расширение `.prs` (Properties File Extension)
- Действия требуют интерактивного взаимодействия (выбор файла через диалог)

## 📋 Подход к миграции через Export/Import

### Вариант 1: Использование действий (требует доработки)
1. **Экспорт с исходного сервера**:
   - Выполнить действие `export` на контексте (например, `users.admin.models.objects`)
   - Сохранить файл `.prs` локально
   - Повторить для всех контекстов

2. **Импорт на целевой сервер**:
   - Выполнить действие `import` на целевом контексте
   - Загрузить файл `.prs`
   - Повторить для всех контекстов

**Проблема**: Действия требуют интерактивного взаимодействия (диалог выбора файла), что затрудняет автоматизацию.

### Вариант 2: Прямая работа с функциями (если доступны)
Нужно найти функции, которые работают напрямую с файлами без интерактивного взаимодействия.

## 🔍 Что нужно найти

1. **Функции для работы с файлами**:
   - Функция для экспорта контекста в файл (без диалога)
   - Функция для импорта контекста из файла (без диалога)
   - Параметры этих функций (путь к файлу, формат и т.д.)

2. **Формат PRS файла**:
   - Структура файла `.prs`
   - Как он хранит данные контекста (переменные, функции, события, тип модели, группа и т.д.)

3. **Реализация на сервере**:
   - Где находится код, который выполняет экспорт/импорт
   - Можно ли вызвать его напрямую через функции

## 💡 Рекомендации

1. **Изучить клиент AggreGate**:
   - Найти, как клиент обрабатывает действия `export`/`import`
   - Понять, какие команды отправляются на сервер
   - Найти функции, которые можно вызвать напрямую

2. **Альтернативный подход**:
   - Если действия требуют интерактивного взаимодействия, можно:
     - Использовать существующий подход (получение данных и установка через `set_variable`)
     - Или найти функции, которые работают напрямую с файлами

3. **Гибридный подход**:
   - Использовать export/import для простых контекстов
   - Использовать прямой подход для сложных (модели с типом, компонентами)

## 📊 Текущий статус

- ✅ Действия `export`/`import` найдены и доступны
- ✅ Действие `export` успешно выполнилось
- ⏳ Требуется найти способ автоматизации (без интерактивного взаимодействия)
- ⏳ Требуется изучить формат PRS файла

## 🔄 Следующие шаги

1. Найти функции для прямого экспорта/импорта (без диалогов)
2. Изучить формат PRS файла
3. Протестировать экспорт/импорт одного контекста
4. Применить ко всем контекстам

