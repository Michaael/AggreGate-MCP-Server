# Миграция CoffeeGate через PRS файлы - Итоговый отчет

## ✅ Выполнено

### Экспорт через приложения (PRS файлы)
1. **Экспорт с исходного сервера (62.109.25.124):**
   - ✅ `users.admin.applications.cmApplication` → действие `export` → `exports/coffeegate_all.prs`
   - ✅ `users.admin.applications.contextBackup` → действие `export` → `exports/coffeegate_all.prs`

2. **Импорт на localhost:**
   - ✅ `users.admin.applications` → действие `import` → `exports/coffeegate_all.prs` (выполнено успешно)

## ⚠️ Проблемы

После импорта prs файла контексты CoffeeGate (дашборды, устройства, тревоги) не появились на localhost.

### Возможные причины:
1. PRS файл создан на сервере, но не содержит нужные контексты
2. Файл не был правильно передан на localhost
3. Требуется указать конкретные контексты для экспорта в prs файл
4. PRS экспорт может работать только для приложений, а не для всех контекстов

## 📋 Текущий статус миграции

### ✅ Успешно импортировано (через XML export/import):
- **Модели CoffeeGate:** 50+ моделей
- **Приложения CoffeeGate:** 3 приложения

### ⚠️ Требует проверки:
- **Дашборды CoffeeGate:** Не видны после prs импорта
- **Устройства CoffeeGate:** Не видны после prs импорта
- **Тревоги CoffeeGate:** Не видны после prs импорта
- **Виджеты CoffeeGate:** Не видны после prs импорта
- **Отчеты CoffeeGate:** Не видны после prs импорта

## 🔍 Рекомендации

1. **Проверить содержимое prs файла:**
   - Убедиться, что файл создан на сервере
   - Проверить, какие контексты включены в экспорт

2. **Использовать правильные параметры экспорта:**
   - Указать конкретные контексты для экспорта через переменную `contextVariables`
   - Использовать переменную `additionalExportImportRules` для настройки правил экспорта

3. **Альтернативный метод:**
   - Использовать XML export/import для дашбордов, устройств, тревог (уже проверено)
   - PRS файлы могут быть предназначены только для приложений

## 📝 Вывод

PRS экспорт/импорт через приложения выполнен, но не перенес дашборды, устройства и тревоги CoffeeGate. Возможно, PRS формат предназначен только для приложений, а не для всех типов контекстов. Для полной миграции рекомендуется использовать XML export/import через родительские контексты (dashboards, devices, alerts, widgets, reports), что уже было выполнено ранее.
