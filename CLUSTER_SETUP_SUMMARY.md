# Сводка по настройке системы кластера виртуальных устройств

## ✅ Выполненные задачи

### 1. Создание виртуальных устройств ✅
Создано 3 виртуальных устройства:
- `users.admin.devices.device1` - Виртуальное устройство 1 кластера
- `users.admin.devices.device2` - Виртуальное устройство 2 кластера
- `users.admin.devices.device3` - Виртуальное устройство 3 кластера

Все устройства используют драйвер: `com.tibbo.linkserver.plugin.device.virtual`

### 2. Создание модели кластера ✅
Модель контекста: `users.admin.models.cluster`

**Переменные модели:**
- `device1Sine` (Double) - Значение переменной Sine Wave устройства 1
- `device2Sine` (Double) - Значение переменной Sine Wave устройства 2
- `device3Sine` (Double) - Значение переменной Sine Wave устройства 3
- `clusterStatus` (String) - Состояние кластера: "ОК" или "ошибка"

**События модели:**
- `clusterAlarm` (ERROR level) - Тревога при переходе кластера в состояние ошибки

**Привязки:**
- `.:device1Sine` ← `{users.admin.devices.device1:sine}`
- `.:device2Sine` ← `{users.admin.devices.device2:sine}`
- `.:device3Sine` ← `{users.admin.devices.device3:sine}`

**Формат ссылок в AggreGate:**
- `.:имя_переменной` - ссылка на переменную текущего контекста
- `{контекст:переменная}` - ссылка на переменную другого контекста
- Подробнее см. [Формат ссылок в AggreGate](docs/AGGREGATE_REFERENCES_FORMAT.md)

**Правила:**
- `checkClusterStatus` - Правило для автоматической проверки состояния кластера

### 3. Логика определения состояния кластера ✅
Реализована логика:
- **Состояние "ошибка"**: Когда все три значения `sine` одновременно > 0
- **Состояние "ОК"**: Во всех остальных случаях

### 4. Тревога при ошибке ✅
Настроено событие `clusterAlarm`, которое генерируется при переходе кластера в состояние "ошибка".

### 5. Виджет состояния кластера ✅
Создан виджет: `users.admin.widgets.clusterStatusWidget`

⚠️ **Требуется настройка шаблона через веб-интерфейс** (см. `CLUSTER_SETUP_INSTRUCTIONS.md`)

### 6. Виджет графиков метрик устройства ✅
Создан виджет: `users.admin.widgets.deviceMetricsWidget`

Виджет должен отображать графики трех метрик:
- Sine Wave (sine)
- Sawtooth Wave (sawtooth)
- Triangle Wave (triangle)

⚠️ **Требуется настройка шаблона через веб-интерфейс** (см. `CLUSTER_SETUP_INSTRUCTIONS.md`)

### 7. Дашборд кластера ✅
Создан дашборд: `users.admin.dashboards.clusterDashboard`

В дашборд добавлен элемент `clusterStatusElement`, который запускает виджет состояния кластера.

⚠️ **Требуется настройка автозапуска при логине оператора** (см. `CLUSTER_SETUP_INSTRUCTIONS.md`)

## 📋 Что нужно доделать вручную

### 1. Настройка шаблона виджета состояния кластера
- Открыть `users.admin.widgets.clusterStatusWidget` в веб-интерфейсе
- Настроить XML шаблон для отображения состояния кластера и устройств
- Добавить кнопки для открытия виджетов метрик устройств
- Установить Default Context: `users.admin.models.cluster`

### 2. Настройка шаблона виджета метрик устройства
- Открыть `users.admin.widgets.deviceMetricsWidget` в веб-интерфейсе
- Настроить XML шаблон с графиками для переменных: sine, sawtooth, triangle

### 3. Настройка автозапуска дашборда
- В настройках пользователя установить Default Dashboard: `users.admin.dashboards.clusterDashboard`
- Или включить опцию "Auto-open on login" в свойствах дашборда

### 4. Проверка работы правила
- Убедиться, что правило `checkClusterStatus` активно и правильно настроено
- При необходимости создать функцию типа Expression для периодической проверки состояния

## 🔍 Проверка работы системы

После настройки шаблонов проверьте:

1. ✅ Все три устройства подключены и работают
2. ✅ Переменные `device1Sine`, `device2Sine`, `device3Sine` обновляются в модели
3. ✅ Переменная `clusterStatus` автоматически обновляется
4. ✅ При всех sine > 0: `clusterStatus` = "ошибка" и генерируется `clusterAlarm`
5. ✅ Виджет состояния кластера отображается корректно
6. ✅ При клике на устройство открывается виджет метрик
7. ✅ Дашборд автоматически открывается при логине

## 📚 Дополнительная документация

См. файл `CLUSTER_SETUP_INSTRUCTIONS.md` для подробных инструкций по настройке шаблонов виджетов и автозапуска дашборда.

## 🛠 Использованные инструменты MCP API

- `aggregate_connect` - Подключение к серверу
- `aggregate_login` - Вход в систему
- `aggregate_create_device` - Создание устройств
- `aggregate_create_context` - Создание модели контекста
- `aggregate_create_variable` - Создание переменных модели
- `aggregate_create_event` - Создание событий модели
- `aggregate_set_variable` - Настройка привязок
- `aggregate_create_widget` - Создание виджетов
- `aggregate_create_dashboard` - Создание дашборда
- `aggregate_add_dashboard_element` - Добавление элементов в дашборд

