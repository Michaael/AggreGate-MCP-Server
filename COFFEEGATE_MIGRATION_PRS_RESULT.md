# Результат миграции CoffeeGate через PRS файлы

## ✅ Выполнено

### Экспорт через приложения (PRS файлы)
- ✅ `users.admin.applications.cmApplication` → `export` → `exports/coffeegate_all.prs`
- ✅ `users.admin.applications.contextBackup` → `export` → `exports/coffeegate_all.prs`

## ⚠️ Проблемы с импортом

Импорт через действие `import` в контексте приложения не работает:
- ❌ `users.admin.applications.contextBackup` → действие `import` не доступно
- ❌ `users.admin.applications.cmApplication` → требуется проверка доступности действия

## 📋 Текущий статус

- **Модели CoffeeGate:** ✅ Все 50+ моделей импортированы на localhost (через XML export/import)
- **Приложения CoffeeGate:** ✅ Экспортированы через XML
- **Дашборды, устройства, тревоги, виджеты, отчеты:** ⚠️ Требуется проверка импорта через PRS

## 🔍 Рекомендации

1. Проверить создание prs файлов на сервере
2. Найти правильный способ импорта prs файлов (возможно через веб-интерфейс или другой API метод)
3. Проверить, какие контексты были включены в prs экспорт
