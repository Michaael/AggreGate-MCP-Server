# Лучшие практики тестирования и работы с MCP сервером

## ⚠️ Критические правила для избежания ошибок

### 1. Создание функций (Expression)

**ОБЯЗАТЕЛЬНЫЙ рабочий процесс:**

1. **ВСЕГДА используйте `aggregate_build_expression`** перед созданием функции
2. **ВСЕГДА используйте `aggregate_validate_expression`** перед созданием
3. **Проверяйте существование функции** перед созданием через `aggregate_get_function`
4. **Для функций с несколькими полями** используйте `<<>>` в inputFormat/outputFormat
5. **В expression ВСЕГДА используйте `<<>>`** внутри table()

**Пример:**
```python
# Шаг 1: Построение форматов
build_expr = aggregate_build_expression(
    inputFields=[{"name": "value1", "type": "E"}, {"name": "value2", "type": "E"}],
    outputFields=[{"name": "result", "type": "E"}],
    formula="({value1} + {value2}) / 2"
)

# Шаг 2: Валидация
validate = aggregate_validate_expression(
    inputFormat=build_expr["inputFormat"],
    outputFormat=build_expr["outputFormat"],
    expression=build_expr["expression"]
)

# Шаг 3: Проверка существования
existing = aggregate_get_function(path, "functionName")
if existing:
    # Функция уже существует
    pass
else:
    # Шаг 4: Создание
    create = aggregate_create_function(
        path=path,
        functionName="functionName",
        functionType=1,
        inputFormat=build_expr["inputFormat"],
        outputFormat=build_expr["outputFormat"],
        expression=build_expr["expression"]
    )
```

### 2. Создание событий

**ОБЯЗАТЕЛЬНЫЙ рабочий процесс:**

1. **Проверяйте существование события** перед созданием через `aggregate_list_events`
2. **Используйте правильные форматы** для событий с параметрами
3. **Для генерации событий из контекста** используйте `aggregate_fire_context_event` (не `aggregate_fire_event`)

**Пример:**
```python
# Шаг 1: Проверка существования
events = aggregate_list_events(path)
existing_events = [e["name"] for e in events["events"]]

# Шаг 2: Создание (если не существует)
if "eventName" not in existing_events:
    create = aggregate_create_event(
        path=path,
        eventName="eventName",
        format="<temperature><E><message><S>",
        level=1  # WARNING
    )

# Шаг 3: Генерация события из контекста
fire = aggregate_fire_context_event(
    path=path,
    eventName="eventName",
    level="WARNING",
    data={
        "records": [{"temperature": 35.5, "message": "High temp"}],
        "format": {"fields": [{"name": "temperature", "type": "E"}, {"name": "message", "type": "S"}]}
    }
)
```

### 3. Создание правил

**ОБЯЗАТЕЛЬНЫЙ рабочий процесс:**

1. **Проверяйте существование правила** перед созданием через `aggregate_list_rules`
2. **Используйте упрощенный синтаксис** для выражений правил
3. **Избегайте сложных выражений** с генерацией событий в правилах (используйте алармы вместо этого)

**Пример:**
```python
# Шаг 1: Проверка существования
rules = aggregate_list_rules(path)
existing_rules = [r["name"] for r in rules["rules"]]

# Шаг 2: Создание простого правила
if "ruleName" not in existing_rules:
    create = aggregate_create_rule(
        path=path,
        ruleName="ruleName",
        trigger="temperature",  # Переменная-триггер
        expression="counter = counter + 1;",  # Простое выражение
        enabled=True
    )
```

**Правильный синтаксис правил:**
- ✅ Простые присваивания: `counter = counter + 1;`
- ✅ Условные выражения: `if (temperature > 30) { status = 'high'; }`
- ❌ Избегайте: сложных вызовов функций, генерации событий через `fireEvent()`

### 4. Создание переменных

**ОБЯЗАТЕЛЬНЫЙ рабочий процесс:**

1. **Проверяйте существование переменной** перед созданием через `aggregate_list_variables`
2. **Используйте `aggregate_set_variable_field`** для установки значений простых переменных
3. **Используйте `aggregate_set_variable`** для установки значений сложных переменных (с несколькими полями)

**Пример:**
```python
# Шаг 1: Проверка существования
variables = aggregate_list_variables(path)
existing_vars = [v["name"] for v in variables["variables"]]

# Шаг 2: Создание (если не существует)
if "variableName" not in existing_vars:
    create = aggregate_create_variable(
        path=path,
        variableName="variableName",
        format="<value><E>",
        writable=True
    )

# Шаг 3: Установка значения (для простых переменных)
set_value = aggregate_set_variable_field(
    path=path,
    variableName="variableName",
    fieldName="value",
    value=25.5
)
```

### 5. Создание привязок (Bindings)

**ОБЯЗАТЕЛЬНЫЙ рабочий процесс:**

1. **Для абсолютных моделей** используйте абсолютные пути: `{users.admin.devices.device1:sine}`
2. **Для относительных моделей** используйте относительные ссылки: `{.:sine}`
3. **Всегда указывайте `target`** с префиксом `.:` для переменной в модели

**Пример:**
```python
# Абсолютная модель
bindings = {
    "records": [{
        "target": ".:temperature",
        "expression": "{users.admin.devices.device1:sine}",
        "onevent": True
    }],
    "format": {
        "fields": [
            {"name": "target", "type": "S"},
            {"name": "expression", "type": "S"},
            {"name": "onevent", "type": "B"}
        ]
    }
}
aggregate_set_variable(path=model_path, name="bindings", value=bindings)

# Относительная модель
bindings = {
    "records": [{
        "target": ".:sumWaves",
        "expression": "{.:sine} + {.:sawtooth}",  # Относительные ссылки!
        "onevent": True
    }]
}
aggregate_set_variable(path=model_path, name="bindings", value=bindings)
```

## 📋 Чек-лист перед созданием элементов

### Перед созданием функции:
- [ ] Использован `aggregate_build_expression` для построения форматов
- [ ] Использован `aggregate_validate_expression` для валидации
- [ ] Проверено существование функции через `aggregate_get_function`
- [ ] Для множественных полей добавлены `<<>>` в форматы
- [ ] В expression использованы `<<>>` внутри table()

### Перед созданием события:
- [ ] Проверено существование события через `aggregate_list_events`
- [ ] Указан правильный формат события
- [ ] Указан правильный уровень события (0=INFO, 1=WARNING, 2=ERROR, 3=FATAL, 4=NOTICE)

### Перед созданием правила:
- [ ] Проверено существование правила через `aggregate_list_rules`
- [ ] Использован упрощенный синтаксис выражения
- [ ] Избегаются сложные вызовы функций

### Перед созданием переменной:
- [ ] Проверено существование переменной через `aggregate_list_variables`
- [ ] Указан правильный формат переменной
- [ ] Установлен `writable=True` если переменная будет обновляться через привязки

## 🔧 Инструменты для отладки

### При ошибках создания функции:
1. Используйте `aggregate_explain_error` для понимания ошибки
2. Используйте `aggregate_fix_function_parameters` для исправления параметров
3. Проверьте созданную функцию через `aggregate_get_function`

### При ошибках генерации событий:
1. Убедитесь, что событие создано через `aggregate_list_events`
2. Используйте `aggregate_fire_context_event` для контекстов (не `aggregate_fire_event`)
3. Проверьте формат данных события

### При ошибках создания правил:
1. Упростите выражение правила
2. Проверьте синтаксис JavaScript/Expression
3. Используйте простые присваивания вместо сложных выражений

## 📝 Рекомендации для будущей работы

1. **Всегда проверяйте существование** элементов перед созданием
2. **Используйте правильные инструменты** для разных типов операций
3. **Следуйте обязательному рабочему процессу** для каждого типа элемента
4. **Используйте упрощенный синтаксис** для правил и выражений
5. **Тестируйте после создания** каждого элемента

---

**Версия:** 1.0.0  
**Дата:** 2025-01-27  
**Статус:** ✅ Актуально
