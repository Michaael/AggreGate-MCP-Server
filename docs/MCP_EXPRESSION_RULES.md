# Правила работы с Expression функциями AggreGate через MCP

## ⚠️ Критические правила (встроены в инструменты)

### Правило 1: Форматы для Expression функций

**⚠️ ВАЖНО: Разные правила для одного и множественных полей!**

#### Для функций с ОДНИМ полем (БЕЗ <<>>):
✅ **ПРАВИЛЬНО:**
```
inputFormat: "<value><E>"
outputFormat: "<result><E>"
```

#### Для функций с НЕСКОЛЬКИМИ полями (С <<>>):
✅ **ПРАВИЛЬНО:**
```
inputFormat: "<<value1><E><value2><E>>"  // С <<>> для множественных полей!
outputFormat: "<<result><E>>"
```

❌ **НЕПРАВИЛЬНО (для множественных полей):**
```
inputFormat: "<value1><E><value2><E>"  // ОШИБКА! Теряет второе поле
```

**Почему:** AggreGate неправильно парсит формат `<value1><E><value2><E>` без `<<>>` и теряет поля после первого. Формат `<<value1><E><value2><E>>` парсится правильно.

**Проверка:** После создания функции всегда проверяйте через `aggregate_get_function` - если inputFormat содержит только одно поле вместо нескольких, используйте формат с `<<>>`.

### Правило 2: Expression С <<>> внутри table()
**expression ДОЛЖЕН содержать <<>> скобки ВНУТРИ функции table()!**

✅ **ПРАВИЛЬНО:**
```
expression: "table(\"<<result><E>>\", ({value1} + {value2}) / 2)"
```

❌ **НЕПРАВИЛЬНО:**
```
expression: "table(\"<result><E>\", ({value1} + {value2}) / 2)"  // ОШИБКА - нет <<>>
```

## 🔧 Рекомендуемый рабочий процесс для ИИ

### Шаг 1: Построение правильных форматов
**ВСЕГДА используйте `aggregate_build_expression` перед созданием Expression функции!**

```json
{
  "tool": "aggregate_build_expression",
  "parameters": {
    "inputFields": [
      {"name": "value1", "type": "E", "description": "Первое значение"},
      {"name": "value2", "type": "E", "description": "Второе значение"}
    ],
    "outputFields": [
      {"name": "result", "type": "E", "description": "Результат"}
    ],
    "formula": "({value1} + {value2}) / 2"
  }
}
```

**Результат:**
```json
{
  "success": true,
  "inputFormat": "<value1><E><value2><E>",
  "outputFormat": "<result><E>",
  "encodedInputFormat": "<<value1><E><value2><E>>",  // ✅ НОВОЕ: безопасный формат для множественных полей
  "encodedOutputFormat": "<result><E>",  // ✅ НОВОЕ: для одного поля = обычный формат
  "expression": "table(\"<<result><E>>\", ({value1} + {value2}) / 2)",
  "usage": {
    "step1": "Use these values in aggregate_create_function:",
    "step2": "Set functionType to 1 (Expression)",
    "step3": "For multiple fields: use encodedInputFormat/encodedOutputFormat (with <<>>)",
    "step4": "For single field: use inputFormat/outputFormat (without <<>>)",
    "step5": "Use expression AS-IS (it already has <<>> inside table())",
    "note": "aggregate_create_function now auto-fixes formats, but using encoded formats is recommended"
  }
}
```

**✨ Улучшение:** Инструмент теперь возвращает **два варианта форматов**:
- `inputFormat` / `outputFormat` - базовые форматы (без `<<>>`)
- `encodedInputFormat` / `encodedOutputFormat` - безопасные форматы для множественных полей (с `<<>>`)

**Рекомендация:** Для функций с **несколькими полями** используйте `encodedInputFormat` / `encodedOutputFormat` - они гарантированно сохранят все поля.

### Шаг 2: Валидация перед созданием
**ВСЕГДА используйте `aggregate_validate_expression` перед `aggregate_create_function`!**

```json
{
  "tool": "aggregate_validate_expression",
  "parameters": {
    "inputFormat": "<value1><E><value2><E>",
    "outputFormat": "<result><E>",
    "expression": "table(\"<<result><E>>\", ({value1} + {value2}) / 2)"
  }
}
```

**Результат:**
```json
{
  "valid": true,
  "errors": [],
  "warnings": [],
  "suggestions": [],
  "normalizedInputFormat": "<value1><E><value2><E>",  // ✅ НОВОЕ: нормализованный формат
  "normalizedOutputFormat": "<result><E>",  // ✅ НОВОЕ: нормализованный формат
  "note": "Formats were normalized - use normalizedInputFormat/normalizedOutputFormat in aggregate_create_function"
}
```

**✨ Улучшение:** Инструмент теперь **автоматически нормализует** форматы:
- Если вы передали формат с `<<>>` - он будет нормализован (убраны внешние скобки)
- Если вы передали формат без `<<>>` - он останется как есть
- В результате всегда возвращаются `normalizedInputFormat` и `normalizedOutputFormat` - готовые к использованию

Если есть ошибки, инструмент покажет:
- Что именно неверно
- Правильный пример
- Конкретные рекомендации
- Нормализованные форматы (даже при ошибках)

### Шаг 3: Создание функции
**Только после успешной валидации вызывайте `aggregate_create_function`!**

**Для функций с одним полем:**
```json
{
  "tool": "aggregate_create_function",
  "parameters": {
    "path": "users.admin.models.calculator",
    "functionName": "calculate_single",
    "functionType": 1,
    "inputFormat": "<value><E>",
    "outputFormat": "<result><E>",
    "expression": "table(\"<<result><E>>\", {value} * 2)",
    "description": "Удвоение значения"
  }
}
```

**Для функций с несколькими полями (ИСПОЛЬЗУЙТЕ <<>>):**
```json
{
  "tool": "aggregate_create_function",
  "parameters": {
    "path": "users.admin.models.calculator",
    "functionName": "calculate_average",
    "functionType": 1,
    "inputFormat": "<<value1><E><value2><E>>",
    "outputFormat": "<<result><E>>",
    "expression": "table(\"<<result><E>>\", ({value1} + {value2}) / 2)",
    "description": "Вычисление среднего значения"
  }
}
```

**✨ Улучшение:** `aggregate_create_function` теперь **автоматически исправляет** форматы:
- Если формат без `<<>>` теряет поля при парсинге - инструмент автоматически перепарсит его с правильными настройками
- Это означает, что даже "неправильный" формат `<value1><E><value2><E>` будет работать корректно
- Однако, **рекомендуется** использовать `encodedInputFormat` / `encodedOutputFormat` из `aggregate_build_expression` для гарантии

**⚠️ После создания проверьте inputFormat через `aggregate_get_function` - все поля должны присутствовать.**

## 🛡️ Автоматическая обработка ошибок

Если при создании функции возникает ошибка:

1. **Вызовите `aggregate_explain_error`:**
```json
{
  "tool": "aggregate_explain_error",
  "parameters": {
    "message": "Invalid inputFormat: <<value1><E>>",
    "toolName": "aggregate_create_function"
  }
}
```

2. **Инструмент вернёт:**
```json
{
  "category": "function_format",
  "explanation": "Неверный формат... inputFormat/outputFormat задаются БЕЗ <<>>",
  "detailedExplanation": "Обнаружены <<>> в inputFormat или outputFormat - это ОШИБКА!",
  "recommendation": "1. Используйте aggregate_build_expression... 2. Используйте aggregate_validate_expression..."
}
```

3. **Исправьте ошибку и повторите с шага 1**

## 📋 Типы полей TableFormat

- `S` - String (строка)
- `I` - Integer (целое число)
- `E` - Number/Double (число с плавающей точкой, рекомендуется)
- `B` - Boolean (логическое значение)
- `L` - Long (длинное целое)
- `T` - DataTable (таблица данных)

## ✅ Чек-лист для ИИ

Перед созданием Expression функции:

- [ ] Использовал `aggregate_build_expression` для генерации форматов
- [ ] Определил количество полей:
  - **Одно поле** → используйте `inputFormat` / `outputFormat` (без `<<>>`)
  - **Несколько полей** → используйте `encodedInputFormat` / `encodedOutputFormat` (с `<<>>`) ✅ РЕКОМЕНДУЕТСЯ
- [ ] (Опционально) Использовал `aggregate_validate_expression` для проверки
  - Используйте `normalizedInputFormat` / `normalizedOutputFormat` из результата валидации
- [ ] Убедился, что `expression` С <<>> внутри table()
- [ ] Все имена полей в expression соответствуют inputFormat
- [ ] Вызвал `aggregate_create_function` с правильными форматами
- [ ] После создания проверил через `aggregate_get_function` - все поля присутствуют
- [ ] Если ошибка - использовал `aggregate_explain_error` для диагностики

**✨ Новое:** Благодаря автоматическому исправлению форматов, большинство ошибок теперь исправляются автоматически!

## 🎯 Пример полного цикла

```json
// 1. Построение
{"tool": "aggregate_build_expression", "parameters": {...}}

// 2. Валидация
{"tool": "aggregate_validate_expression", "parameters": {...}}

// 3. Создание
{"tool": "aggregate_create_function", "parameters": {...}}

// 4. Если ошибка - объяснение
{"tool": "aggregate_explain_error", "parameters": {...}}

// 5. Тестирование
{"tool": "aggregate_test_function", "parameters": {...}}
```

**Эти правила встроены в сами инструменты через описания и мета-информацию, поэтому ИИ не может их "забыть" - они всегда доступны через `aggregate_list_tools`!**
