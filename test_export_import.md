# Тестирование Export/Import

## Дата: 2025-01-27

## ✅ Сборка завершена

MCP сервер успешно пересобран с поддержкой `filePath` для export/import действий.

## 📋 Инструкции по тестированию

### Предварительные требования

1. Запущен MCP сервер
2. Подключение к исходному AggreGate серверу (connectionKey: "source")
3. Подключение к целевому AggreGate серверу (connectionKey: "target")
4. Создана директория для экспорта: `C:/migration/export/`

### Тест 1: Экспорт простого контекста

```json
{
  "path": "users.admin.models",
  "actionName": "export",
  "filePath": "C:/migration/export/models_test.prs",
  "connectionKey": "source"
}
```

**Ожидаемый результат:**
- Действие выполнилось успешно
- Файл `models_test.prs` создан в указанной директории
- В логах видно установку пути к файлу

### Тест 2: Экспорт модели objects

```json
{
  "path": "users.admin.models.objects",
  "actionName": "export",
  "filePath": "C:/migration/export/objects.prs",
  "connectionKey": "source"
}
```

**Ожидаемый результат:**
- Действие выполнилось успешно
- Файл `objects.prs` создан
- Файл содержит все данные модели (переменные, функции, события, привязки)

### Тест 3: Импорт модели objects

```json
{
  "path": "users.admin.models",
  "actionName": "import",
  "filePath": "C:/migration/export/objects.prs",
  "connectionKey": "target"
}
```

**Ожидаемый результат:**
- Действие выполнилось успешно
- Модель `objects` создана на целевом сервере
- Все компоненты модели перенесены корректно

### Тест 4: Проверка корректности переноса

После импорта проверить:
1. Тип модели (абсолютная, относительная, экземплярная)
2. Группа модели
3. Переменные модели
4. Функции модели
5. События модели
6. Привязки
7. Наборы правил

## 🔍 Отладка

### Логи для проверки

При выполнении export/import в логах должны быть:
```
Action command received: editData (title: ...)
Command parameters: ...
Set file path field 'file' to: C:/migration/export/objects.prs
```

### Возможные проблемы

1. **Файл не создается**
   - Проверить права доступа к директории
   - Проверить, что путь абсолютный
   - Проверить логи на ошибки

2. **Поле для пути не найдено**
   - Проверить структуру данных в логах
   - Возможно, нужно использовать другое имя поля

3. **Импорт не работает**
   - Проверить, что файл существует
   - Проверить формат файла (.prs)
   - Проверить права на чтение файла

## 📝 Следующие шаги

После успешного тестирования:
1. Экспортировать все модели с исходного сервера
2. Импортировать все модели на целевой сервер
3. Проверить корректность миграции

