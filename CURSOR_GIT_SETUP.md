# Настройка Git в Cursor

## ✅ Выполнено

### 1. Созданы файлы конфигурации

- **`.vscode/settings.json`** - Настройки Git для Cursor/VS Code
- **`.vscode/extensions.json`** - Рекомендуемые расширения для Git
- **`.gitattributes`** - Правила обработки файлов Git

### 2. Настроены глобальные параметры Git

- `core.autocrlf = true` - Автоматическое преобразование окончаний строк
- `core.editor = code --wait` - Cursor/VS Code как редактор по умолчанию
- `merge.tool = vscode` - Cursor/VS Code как инструмент слияния
- `diff.tool = vscode` - Cursor/VS Code как инструмент сравнения

## 📋 Настройки в Cursor

### Git интеграция

Cursor автоматически обнаружит Git репозиторий и предоставит:

1. **Source Control панель** (Ctrl+Shift+G)
   - Просмотр изменений
   - Стадирование файлов
   - Создание коммитов
   - Push/Pull операции

2. **Git индикаторы в редакторе**
   - Зеленая полоса - новые строки
   - Синяя полоса - измененные строки
   - Красная полоса - удаленные строки

3. **Git команды в Command Palette** (Ctrl+Shift+P)
   - `Git: Clone`
   - `Git: Pull`
   - `Git: Push`
   - `Git: Commit`
   - И другие Git команды

### Рекомендуемые расширения

В `.vscode/extensions.json` добавлены рекомендуемые расширения:

- **GitLens** - Расширенная функциональность Git
- **Git Graph** - Визуализация истории Git
- **Git History** - Просмотр истории файлов
- **GitHub Pull Requests** - Работа с Pull Requests

Для установки расширений:
1. Откройте Extensions (Ctrl+Shift+X)
2. Найдите "Recommended" в списке
3. Установите рекомендуемые расширения

## 🔧 Дополнительные настройки

### Автоматический fetch

Включен автоматический fetch для получения обновлений с сервера:
```json
"git.autofetch": true
```

### Умные коммиты

Включена функция умных коммитов:
```json
"git.enableSmartCommit": true
"git.suggestSmartCommit": true
```

Это позволяет автоматически добавлять все изменения при коммите.

### Обработка окончаний строк

Настроено через `.gitattributes`:
- Unix-стиль (LF) для исходного кода
- Windows-стиль (CRLF) для .bat, .cmd, .ps1 файлов
- Автоматическое определение для остальных

## 📝 Использование

### Основные операции в Cursor

1. **Просмотр изменений**
   - Откройте Source Control панель (Ctrl+Shift+G)
   - Все измененные файлы будут показаны

2. **Стадирование файлов**
   - Нажмите "+" рядом с файлом
   - Или используйте "Stage All Changes"

3. **Создание коммита**
   - Введите сообщение коммита
   - Нажмите Ctrl+Enter или кнопку "Commit"

4. **Push на сервер**
   - Нажмите "..." в Source Control панели
   - Выберите "Push"
   - Или используйте Command Palette: `Git: Push`

5. **Pull с сервера**
   - Нажмите "..." в Source Control панели
   - Выберите "Pull"
   - Или используйте Command Palette: `Git: Pull`

### Горячие клавиши

- `Ctrl+Shift+G` - Открыть Source Control
- `Ctrl+Enter` - Создать коммит
- `Ctrl+Shift+P` - Command Palette (для Git команд)

## ✅ Проверка работы

1. Откройте Source Control панель (Ctrl+Shift+G)
2. Должны быть видны все измененные файлы
3. Попробуйте создать коммит через интерфейс Cursor
4. Проверьте, что push работает

## 🎉 Готово!

Git полностью настроен для работы в Cursor. Все основные операции доступны через интерфейс IDE.

