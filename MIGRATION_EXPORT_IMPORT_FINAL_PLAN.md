# Финальный план использования Export/Import для миграции

## Дата: 2025-01-27

## ✅ Исследование завершено

### Найденные факты:
1. **Действия export/import** существуют в `EditableChildrenContextConstants`:
   - `A_EXPORT = "export"`
   - `A_IMPORT = "import"`
   
2. **Формат файла**: `.prs` (Properties File) - определен в `ApplicationContextConstants.PROPERTIES_FILE_EXTENSION`

3. **Действие export** успешно выполнилось на `users.admin.models` (2 шага)

4. **Модификация ExecuteActionTool**: Добавлено логирование команд для отладки

## 🔍 Проблемы и решения

### Проблема 1: Интерактивность действий
**Проблема**: Действия export/import требуют интерактивного выбора файла через `EditData` команду.

**Решение**: 
- Модифицировать `ExecuteActionTool.processCommand()` для обработки `EditData` с путем к файлу
- Передавать путь к файлу в параметрах `input` при вызове действия

### Проблема 2: Структура данных для выбора файла
**Проблема**: Непонятно, какая структура данных передается в `EditData.CF_DATA` для выбора файла.

**Решение**: 
- Протестировать export с логированием, чтобы увидеть структуру
- Изучить примеры использования в клиенте AggreGate

## 💡 Подходы к реализации

### Вариант 1: Использование export/import через действия (рекомендуется)

#### Преимущества:
- ✅ Сохраняет все данные контекста (переменные, функции, события, привязки, группы)
- ✅ Поддерживает формат `.prs` (стандартный формат AggreGate)
- ✅ Автоматически обрабатывает зависимости

#### Недостатки:
- ⚠️ Требует обработки интерактивных команд
- ⚠️ Нужно передавать путь к файлу

#### Реализация:
1. **Модифицировать ExecuteActionTool**:
   ```java
   // В processCommand добавить обработку EditData с путем к файлу
   if (cmd.getType().equals(ActionUtils.CMD_EDIT_DATA) && cmd instanceof EditData) {
       EditData editDataCmd = (EditData) cmd;
       DataTable data = editDataCmd.getData();
       
       // Если это запрос на выбор файла для export/import
       if (data != null && data.getFieldCount() > 0) {
           // Проверить, есть ли поле для пути к файлу
           // Если есть, установить путь к файлу
           String filePath = "C:/path/to/export.prs";
           // Установить путь в data
           return new GenericActionResponse(data);
       }
   }
   ```

2. **Процесс экспорта**:
   - Вызвать `execute_action` с `actionName="export"` на контексте
   - Обработать `EditData` команду с путем к файлу
   - Сохранить файл `.prs` на диск

3. **Процесс импорта**:
   - Вызвать `execute_action` с `actionName="import"` на целевом контексте
   - Обработать `EditData` команду с путем к файлу
   - Загрузить файл `.prs` с диска

### Вариант 2: Прямой подход (альтернатива)

#### Преимущества:
- ✅ Полный контроль над процессом
- ✅ Нет интерактивных команд

#### Недостатки:
- ⚠️ Нужно вручную переносить все компоненты
- ⚠️ Сложнее обрабатывать зависимости

#### Реализация:
1. Получить все данные контекста:
   - `childInfo` (для типа модели)
   - `info` (для группы)
   - `modelVariables`, `modelFunctions`, `modelEvents`
   - `bindings`, `ruleSets`
   
2. Создать контекст на целевом сервере

3. Установить все переменные и компоненты

## 📋 План реализации

### Этап 1: Тестирование export/import (приоритет)
1. ✅ Модифицировать `ExecuteActionTool` для логирования (выполнено)
2. ⏳ Пересобрать MCP сервер
3. ⏳ Протестировать export на одном контексте
4. ⏳ Изучить структуру команд
5. ⏳ Реализовать обработку выбора файла

### Этап 2: Реализация export
1. Создать функцию `export_context_to_file(path, contextPath)`
2. Вызвать действие `export`
3. Обработать `EditData` с путем к файлу
4. Сохранить файл `.prs`

### Этап 3: Реализация import
1. Создать функцию `import_context_from_file(path, targetContextPath)`
2. Вызвать действие `import`
3. Обработать `EditData` с путем к файлу
4. Загрузить файл `.prs`

### Этап 4: Миграция моделей
1. Экспортировать все модели с исходного сервера
2. Импортировать на целевой сервер
3. Проверить корректность переноса (тип, группа, компоненты)

## 🎯 Рекомендации

1. **Начать с тестирования** на одном простом контексте (не модель)
2. **Использовать export/import** для моделей, так как они содержат много компонентов
3. **Комбинированный подход**: 
   - export/import для моделей
   - Прямой подход для простых контекстов (если export/import окажется сложным)

## 📝 Следующие шаги

1. Пересобрать MCP сервер с логированием
2. Протестировать export на одном контексте
3. Изучить структуру команд EditData
4. Реализовать обработку выбора файла
5. Протестировать полный цикл export/import

