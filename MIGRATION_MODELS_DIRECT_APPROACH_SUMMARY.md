# Сводка: Прямой подход для миграции моделей

## Дата: 2025-01-27

## ✅ Успешно протестировано

### Тест 1: Абсолютная модель (TYPE_ABSOLUTE = 1)
- **Модель**: objects_test
- **Результат**: ✅ Успешно
- **Компоненты перенесены**:
  - ✅ childInfo (type=1)
  - ✅ modelVariables (4 переменные)
  - ✅ modelFunctions (1 функция)
  - ✅ modelEvents (0 - пусто)
  - ✅ bindings (0 - пусто)
  - ✅ ruleSets (0 - пусто)

## 📋 Процесс переноса модели

### Шаг 1: Получение данных с исходного сервера
```python
# Получить все компоненты модели
childInfo = get_variable(path, "childInfo")
info = get_variable(path, "info")  # для группы
modelVariables = get_variable(path, "modelVariables")
modelFunctions = get_variable(path, "modelFunctions")
modelEvents = get_variable(path, "modelEvents")
bindings = get_variable(path, "bindings")
ruleSets = get_variable(path, "ruleSets")
```

### Шаг 2: Создание модели на целевом сервере
```python
# Создать контекст модели
create_context(parentPath="users.admin.models", name="modelName", description="...")
```

### Шаг 3: Установка компонентов
```python
# childInfo устанавливается автоматически при создании, но можно обновить поля
set_variable_field(path, "childInfo", "type", modelType)

# Установить компоненты
set_variable(path, "modelVariables", modelVariables)
set_variable(path, "modelFunctions", modelFunctions)
set_variable(path, "modelEvents", modelEvents)
set_variable(path, "bindings", bindings)
set_variable(path, "ruleSets", ruleSets)
```

## 🎯 Типы моделей

1. **TYPE_ABSOLUTE = 1** (абсолютная): ✅ Протестировано
   - Не имеет defaultContext
   - Независимая модель

2. **TYPE_RELATIVE = 0** (относительная): ⏳ Требуется тестирование
   - Имеет defaultContext
   - Привязана к родительскому контексту

3. **TYPE_INSTANTIABLE = 2** (экземплярная): ⏳ Требуется тестирование
   - Имеет containerType, containerName, objectType
   - Может создавать экземпляры

## ✅ Вывод

**Прямой подход работает для абсолютных моделей!**

Процесс:
1. ✅ Получение данных с исходного сервера
2. ✅ Создание контекста модели
3. ✅ Установка childInfo (тип модели)
4. ✅ Установка modelVariables
5. ✅ Установка modelFunctions
6. ✅ Установка modelEvents, bindings, ruleSets (если есть)

## 📝 Следующие шаги

1. Найти и протестировать относительную модель (type=0)
2. Найти и протестировать экземплярную модель (type=2)
3. Создать универсальную функцию для переноса всех типов моделей

